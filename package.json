{
  "name": "golf-number-generator",
  "version": "1.0.0",
  "description": "Generates (wrong) numbers",
  "author": "RobertDux",
  "scripts": {
    "start": "parcel src/*.html",
    "build": "parcel build src/*.html"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
